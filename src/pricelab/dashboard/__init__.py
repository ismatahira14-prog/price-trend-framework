"""Dashboard support code (data access + theming) used by dashboard/app.py.

Kept dependency-light (no streamlit import here) so it stays testable and so
the core `pricelab` package doesn't require streamlit just to run ingestion.
"""
